moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'popcorn' / 'widgets' / 'movie'

widgets = [
  '__init__.py',
  'button.py',
]

install_data(widgets, install_dir: moduledir)
