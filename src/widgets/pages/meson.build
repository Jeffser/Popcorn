moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'popcorn' / 'widgets' / 'pages'

widgets = [
  '__init__.py',
  'login.py',
]

install_data(widgets, install_dir: moduledir)