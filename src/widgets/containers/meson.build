moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'popcorn' / 'widgets' / 'containers'

widgets = [
  '__init__.py',
  'carousel.py',
]

install_data(widgets, install_dir: moduledir)