moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'popcorn' / 'widgets' / 'series'

widgets = [
  '__init__.py',
  'overview.py',
  'button.py',
  'page.py',
]

install_data(widgets, install_dir: moduledir)
