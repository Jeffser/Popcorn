moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'popcorn' / 'widgets' / 'misc'

widgets = [
  '__init__.py',
  'user_view.py',
  'overview_carousel.py',
]

install_data(widgets, install_dir: moduledir)