moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'popcorn' / 'widgets' / 'player'

widgets = [
  '__init__.py',
  'player.py',
  'page.py',
  'window.py',
]

install_data(widgets, install_dir: moduledir)
